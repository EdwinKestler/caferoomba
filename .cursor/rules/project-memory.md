# Project Memory

Use this repository's Project Memory v2.5 on every run. Follow `AGENTS.md`
and `docs/PROJECT_MEMORY.md`: `ensure` / `status` / `recall` / `search` /
`session-start` at the beginning; `index --incremental` after admitted-file
changes; `session-close` and `consolidate` before finishing.
