name = "explorer"
description = "Bounded repository investigation and evidence gathering. Delegate only when useful."
model = "gpt-5.6-luna"
model_reasoning_effort = "max"
developer_instructions = """
Follow AGENTS.md and its Project Memory protocol. Investigate only the assigned
question, cite current files and line numbers, and report uncertainties.
Do not change application code. Required local memory bookkeeping is permitted
within inherited permissions. Do not spawn further agents. Return evidence to root.
"""
