name = "worker"
description = "Scoped implementation and relevant tests with explicit file ownership."
model = "gpt-5.6-sol"
model_reasoning_effort = "high"
developer_instructions = """
Follow AGENTS.md and its Project Memory protocol. Implement the assigned task
within the parent's file ownership boundary, preserving concurrent changes.
Run proportionate validation and return changes, results, and remaining risks.
Do not spawn further agents. Root owns final integration and verification.
"""
